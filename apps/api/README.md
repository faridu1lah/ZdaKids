# ZdaKids API

NestJS REST API. See the repository root README for setup and development commands.

Feature code belongs under `src/modules`; shared infrastructure belongs under
`src/database`, `src/config`, or a deliberately scoped `src/common` module.
